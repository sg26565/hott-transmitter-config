<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <artifactId>parent</artifactId>
    <groupId>de.treichels.hott</groupId>
    <version>0.6-SNAPSHOT</version>
    <relativePath>../parent/pom.xml</relativePath>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>HoTTGUI</artifactId>
  <name>HoTT Transmitter Config - Graphical User Interface</name>
  <build>
    <finalName>${project.artifactId}-${project.version}-r${buildNumber}</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <minimizeJar>true</minimizeJar>
          <filters>
            <filter>
              <artifact>org.apache.xmlgraphics:batik-css</artifact>
              <includes>
                <include>org/apache/batik/css/**</include>
              </includes>
            </filter>
            <filter>
              <artifact>org.apache.xmlgraphics:batik-codec</artifact>
              <includes>
                <include>org/apache/batik/ext/awt/image/codec/png/**</include>
                <include>org/apache/batik/ext/awt/image/codec/util/**</include>
              </includes>
            </filter>
          </filters>
          <transformers>
            <transformer>
              <mainClass>gde.mdl.ui.Launcher</mainClass>
            </transformer>
          </transformers>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>assemble</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <descriptors>
            <descriptor>src/main/assembly/bin.xml</descriptor>
          </descriptors>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.googlecode.maven-gcu-plugin</groupId>
        <artifactId>maven-gcu-plugin</artifactId>
        <executions>
          <execution>
            <id>upload</id>
            <phase>install</phase>
            <goals>
              <goal>upload</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <serverId>googlecode</serverId>
          <failsOnError>true</failsOnError>
          <projectName>hott-transmitter-config</projectName>
          <uploads>
            <upload>
              <file>${project.build.directory}/${project.artifactId}-${project.version}-r${buildNumber}-bin.zip</file>
              <summary>${project.artifactId} version ${project.version}</summary>
              <labels>
                <label>Experimental</label>
                <label>Type-Archive</label>
              </labels>
            </upload>
          </uploads>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.eclipse.swt</groupId>
      <artifactId>swt-gtk-linux-x86_64</artifactId>
      <version>4.2.2</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.11</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>hamcrest-core</artifactId>
          <groupId>org.hamcrest</groupId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>
</project>

